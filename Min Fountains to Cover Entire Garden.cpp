#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n;
    cin >> n;
    vector<int> fountains(n);
    for(int i = 0; i < n; i++) {
        cin >> fountains[i];
    }
    vector<int> dp(n + 1, 0);
    for(int i = 1; i <= n; i++) {
        
    }

}

int main() {
    int tt = 1;
    // cin >> tt;
    while(tt--) {
        solve();
    }
}